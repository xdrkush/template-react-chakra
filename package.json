{
    "name": "template",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "front:dev": "npm run start --workspace=front",
        "front:build": "npm run build --workspace=front",
        "back": "npm run start --workspace=back"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}